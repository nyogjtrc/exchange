syntax = "proto3";

package exchange;

service ExchangeService {
    rpc GetRate(RateRequest) returns (RateReply) {}
}

message RateRequest {
    string base = 1;
    string target = 2;
}

message RateReply {
    string base = 1;
    string target = 2;
    double rate = 3;
}
